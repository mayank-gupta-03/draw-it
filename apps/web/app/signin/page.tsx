import React from "react";
import { LoginUserRequestBody } from "@repo/common/api-types";

const page = () => {
  const initialData: LoginUserRequestBody = {
    username: "",
    password: "",
  };

  const onSubmit = () => {};
  return <div></div>;
};

export default page;
